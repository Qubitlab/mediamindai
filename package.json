{
  "name": "mediamindai",
  "version": "1.0.0",
  "private": true,
  "description": "Media Mind AI Group website",
  "scripts": {
    "lint:html": "htmlhint '*.html'",
    "lint:css": "stylelint '**/*.css' --ignore-pattern 'node_modules/' --ignore-pattern 'bootstrap/' --ignore-pattern 'assets/bootstrap/'",
    "lint": "npm run lint:html && npm run lint:css",
    "test": "echo 'Static site — no tests configured' && exit 0"
  },
  "devDependencies": {
    "htmlhint": "^1.1.4",
    "stylelint": "^16.2.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
